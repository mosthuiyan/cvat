{
  "compilerOptions": {
    "target": "ESNext", // 使用最新的 ECMAScript 特性
    "lib": ["dom", "dom.iterable", "esnext"], // 要使用的库
    "allowJs": true,  // 是否允许 javascript .js 文件
    "skipLibCheck": true, // 编译器不检查 .d.ts 类型声明文件，可以加快编译速度
    "allowSyntheticDefaultImports": true, // 允许为没有默认导出的模块创建合成的默认导入
    "strict": true, // 启用所有严格的类型检查选项
    "forceConsistentCasingInFileNames": true, // 处理文件名时必须大小写敏感
    "module": "esnext", // 模块代码生成目标, 使用最新的 ES 标准
    "moduleResolution": "node", // 使用基于文件的node.js 风格解析
    "resolveJsonModule": true,  // 允许导入json文件并且视为模块
    "esModuleInterop": true,  // 允许使用更加宽松的导入，导出，比如 Common.js 风格导入
    "isolatedModules": true,  // 将所有文件视为独立的模块
    "noEmit": true, // 不生成输出文件, 仅进行类型检查
    "jsx": "preserve",  // 保留 jxs 标记，不转换为 react 特定的语法
    "baseUrl": "src"  // 导入时的前缀, 非相对模块的导入时候，从baseUrl开始查找模块
  },
  // 这些文件将被包含在编译过程中, 声明过程, 插件, src目录下的文件
  "include": ["./index.d.ts", "src/index.tsx", "src/assets/index.d.ts", "plugins/**/*", "src"]
}
